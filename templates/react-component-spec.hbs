import { shallow } from 'enzyme'
import React from 'react'

import {{name}} from './{{name}}'

shallow(<{{name}} />)

describe('{{name}}', () => {
  it('contains a test', () => {
    expect(true).toBe(false)
  })
})
